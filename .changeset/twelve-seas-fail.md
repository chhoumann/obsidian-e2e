---
"obsidian-e2e": minor
---

Add failure artifact capture for `createObsidianTest()` and `createPluginTest()`, including per-test artifact directories, plugin data snapshots, and best-effort screenshot/DOM/workspace capture on failure.
